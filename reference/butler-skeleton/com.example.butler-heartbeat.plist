<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- com.example.butler-heartbeat.plist — scheduler template (launchd).
     Replace the example paths before installing.
     Equivalent shapes: Linux systemd .timer/.service, Windows Task Scheduler task.
     Contract: stderr is captured (StandardErrorPath); a non-zero exit code fails the job, never swallowed. -->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.example.butler-heartbeat</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/opt/butler/butler-skeleton/heartbeat.sh</string>
    </array>
    <key>StartInterval</key>
    <integer>1800</integer>
    <key>StandardOutPath</key>
    <string>/var/log/butler/heartbeat.log</string>
    <key>StandardErrorPath</key>
    <string>/var/log/butler/heartbeat.err.log</string>
</dict>
</plist>
